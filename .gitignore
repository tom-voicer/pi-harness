# Secrets — never commit
auth.json

# Session logs — personal + large
sessions/

# Dependencies
node_modules/

# Binary tools — not custom (but shell scripts like piper/agent are tracked)
bin/*
!bin/piper
!bin/agent

# Pi internals — not config
convert
write
.DS_Store

# Pi runtime state — local, not config
intercom/
missions/
models-store.json
mcp-cache.json
pi-crash.log
run-history.jsonl
session-namer.json
trust.json
